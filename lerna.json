{
  "version": "0.0.3-test1.1",
  "packages": ["packages/*"],
  "npmClient": "yarn",
  "command": {
    "publish": {
      "ignoreChanges": ["ignored-file", "*.md"],
      "bump": "from-package"
    },
    "version": {
      "allowBranch": "master"
    }
  }
}
