{
  "version": "independent",
  "useWorkspaces": true,
  "packages": [
    "packages/*",
    "examples/*"
  ],
  "npmClient": "yarn",
  "command": {
    "publish": {
      "ignoreChanges": [
        "ignored-file",
        "*.md"
      ],
      "bump": "from-package"
    },
    "version": {
      "allowBranch": "master"
    }
  }
}
